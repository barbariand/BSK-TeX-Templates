\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{bsk_latex_lib/bsk_meeting}[2025/09/27 v1.6 Meeting Module (Robust)]

\RequirePackage{xparse}

% --- Individual Formalia Point Commands ---

\NewDocumentCommand{\moetetsOppnande}{}
{
  \paragraf{§1}{Opening of the meeting}
  {
    The meeting chairperson declared the meeting open.
  }
}

% --- CORRECTED COMMAND ---
\NewDocumentCommand{\valAvJusterare}{}
{
  % FIX: This now uses the globally safe \getOfficial command.
  % No special syntax or environments are needed here, making it robust.
  \paragraf{§2}{Election of adjuster and vote counters}
  {
    To adjust the protocol alongside the chairperson, \getOfficial{just_name} was elected.
  }
}

\NewDocumentCommand{\faststallandeAvForedragningslista}{ O{} }
{
  \paragraf{§3}{Approval of the agenda}
  {
    The agenda was approved according to the distributed proposal.
  }
}

% --- The Main Automated Formalia Command ---
\NewDocumentCommand{\printFormalia}{}{
  \present
  \absent
  \moetetsOppnande
  \valAvJusterare
  \faststallandeAvForedragningslista
}
